variables:
  project: oss-find-source
  SolutionDirectory: src/
  BuildConfiguration: Release

name: OSSGadget_$(SourceBranchName)_$(Date:yyyyMMdd)$(Rev:.r) $(project)
trigger:
  batch: true
  branches:
    include:
    - master
  paths:
    include:
    - src/oss-find-source
    - src/Shared
pr:
  branches:
    include:
    - master
  paths:
    include:
    - Pipelines/oss-find-source.yml
    - src/oss-find-source
    - src/Shared

steps:
- template: oss-core.yml
  parameters:
    project: $(project)