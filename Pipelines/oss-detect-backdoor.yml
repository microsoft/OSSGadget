variables:
  project: oss-detect-backdoor
  SolutionDirectory: src/
  BuildConfiguration: Release

name: OSSGadget_$(SourceBranchName)_$(Date:yyyyMMdd)$(Rev:.r) $(project)
trigger:
  batch: true
  branches:
    include:
    - master
  paths:
    include:
    - src/oss-detect-backdoor
    - src/Shared
pr:
  branches:
    include:
    - master
  paths:
    include:
    - Pipelines/oss-detect-backdoor.yml
    - src/oss-detect-backdoor
    - src/Shared

steps:
- template: oss-core.yml
  parameters:
    project: $(project)