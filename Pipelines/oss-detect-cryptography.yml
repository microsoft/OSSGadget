variables:
  project: oss-detect-cryptography
  SolutionDirectory: src/
  BuildConfiguration: Release

name: OSSGadget_$(SourceBranchName)_$(Date:yyyyMMdd)$(Rev:.r) $(project)
trigger:
  batch: true
  branches:
    include:
    - master
  paths:
    include:
    - src/oss-detect-cryptography
    - src/Shared
pr:
  branches:
    include:
    - master
  paths:
    include:
    - Pipelines/oss-detect-cryptography.yml
    - src/oss-detect-cryptography
    - src/Shared

steps:
- template: oss-core.yml
  parameters:
    project: $(project)